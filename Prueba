import React, { useEffect, useState } from "react";
import { Button } from "reactstrap"; // O el botón que estés utilizando
import { FaSave, FaTimes, FaEdit } from "react-icons/fa";

const YourTableComponent = () => {
    const [showActions, setShowActions] = useState(false);

    useEffect(() => {
        const storedPermissions = localStorage.getItem('userPermissions');
        if (storedPermissions) {
            const permissions = JSON.parse(storedPermissions);
            setShowActions(permissions.edit || permissions.insert);
        }
    }, []);

    const saveChanges = (id) => {
        // Implementar lógica para guardar cambios
    };

    const cancelEdit = () => {
        // Implementar lógica para cancelar la edición
    };

    const startEdit = (row) => {
        // Implementar lógica para comenzar la edición
    };

    return (
        <Table>
            <thead>
                <tr>
                    {/* Otras columnas */}
                    {showActions && <th>Acciones</th>}
                </tr>
            </thead>
            <tbody>
                {data.map((row) => (
                    <tr key={row.id}>
                        {/* Otras celdas */}
                        {showActions && (
                            <td>
                                {editMode === row.id ? (
                                    <div style={{ display: "flex", justifyContent: "center" }}>
                                        <Button onClick={() => saveChanges(row.id)}>
                                            <FaSave />
                                        </Button>
                                        <Button onClick={cancelEdit}>
                                            <FaTimes />
                                        </Button>
                                    </div>
                                ) : (
                                    <Button onClick={() => startEdit(row)}>
                                        <FaEdit />
                                    </Button>
                                )}
                            </td>
                        )}
                    </tr>
                ))}
            </tbody>
        </Table>
    );
};

export default YourTableComponent;